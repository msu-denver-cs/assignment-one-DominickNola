<%= form_with(model: part_inventory, local: true) do |form| %>
  <% if part_inventory.errors.any? %>
    <div id="error_explanation">
      <h2><%= pluralize(part_inventory.errors.count, "error") %> prohibited this part_inventory from being saved:</h2>

      <ul>
      <% part_inventory.errors.full_messages.each do |message| %>
        <li><%= message %></li>
      <% end %>
      </ul>
    </div>
  <% end %>

  <div class="field">
    <%= form.label :name %>
    <%= form.text_field :name %>
  </div>

  <div class="field">
    <%= form.label :inventory %>
    <%= form.text_field :inventory %>
  </div>

  <div class="actions">
    <%= form.submit %>
  </div>
<% end %>
